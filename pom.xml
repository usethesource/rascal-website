<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.rascalmpl</groupId>
    <artifactId>website</artifactId>
    <version>1.0-SNAPSHOT</version>

    <repositories>
        <repository>
            <id>usethesource</id>
            <url>https://releases.usethesource.io/maven/</url>
        </repository>
    </repositories>
    <pluginRepositories>
        <pluginRepository>
            <id>usethesource</id>
            <url>https://releases.usethesource.io/maven/</url>
        </pluginRepository>
    </pluginRepositories>

    <properties>
        <rascal.version>0.28.1</rascal.version>
        <rascal-maven-plugin.version>0.13.2-SNAPSHOT</rascal-maven-plugin.version>
    </properties>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-clean-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <filesets>
                        <fileset>
                            <directory>docs</directory>
                            <excludes>
                                <exclude>index.mdx</exclude>
                            </excludes>
                            <followSymlinks>false</followSymlinks>
                        </fileset>
                        <fileset>
                            <directory>static/assets</directory>
                            <excludes>
                                <exclude>img</exclude>
                            </excludes>
                            <followSymlinks>false</followSymlinks>
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.rascalmpl</groupId>
                                    <artifactId>rascal</artifactId>
                                    <version>${rascal.version}</version>
                                    <outputDirectory>${project.basedir}</outputDirectory>
                                    <includes>docs/**/*.*</includes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.rascalmpl</groupId>
                <artifactId>rascal-maven-plugin</artifactId>
                <version>${rascal-maven-plugin.version}</version>
                <executions>
                    <execution> 
                        <id>default-cli</id> 
                        <phase>compile</phase>
                        <goals>
                            <goal>tutor</goal>
                        </goals>
                        <configuration>
                            <enableStandardLibrary>true</enableStandardLibrary>
                            <errorsAsWarnings>false</errorsAsWarnings>
                            <bin>${project.basedir}</bin> <!-- because tutor appends /docs to this -->
                            <srcs>
                                <src>${project.basedir}/courses/WhyRascal</src>
                                <src>${project.basedir}/courses/Developers</src>
                                <src>${project.basedir}/courses/Recipes</src>
                                <src>${project.basedir}/courses/GettingHelp</src>
                                <src>${project.basedir}/courses/GettingStarted</src>
                                <src>${project.basedir}/courses/RascalConcepts</src>
                                <src>${project.basedir}/courses/RascalShell</src>
                                <src>${project.basedir}/courses/Rascalopedia</src>
                                <src>${project.basedir}/courses/Tutor</src>
                            </srcs>
                            <libs>
                                <lib>|std:///|</lib>
                            </libs>
                        </configuration>
                    </execution> 
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.0.2</version>
                <executions>
                    <execution>
                        <id>copy-assets</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.basedir}/static/assets</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.basedir}/docs/assets</directory>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>org.rascalmpl</groupId>
            <artifactId>rascal</artifactId>
            <version>${rascal.version}</version>
        </dependency>        
    </dependencies>

</project>
